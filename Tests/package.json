{
  "type": "module",
  "scripts": {
    "pretest": "dotnet fable ../ArrayT.fsproj --verbose && dotnet fable --verbose -o dist/tests --nocache" ,
    "test": "mocha dist/tests"
  },
  "dependencies": {
    "mocha": "^10.2.0"
  }
}

{
  "type": "module",
  "scripts": {
    "watchTS": "dotnet fable watch --outDir _ts --lang typescript ../Src/ArrayT.fsproj --run tsc" ,

    "buildTS": "dotnet fable --outDir _ts --noCache  --lang typescript ../Src/ArrayT.fsproj --silent --run tsc" ,
    "pretest": "dotnet fable --outDir _ts --noCache ",
    "test": "mocha _ts && npm run buildTS"
  },

  "devDependencies": {
    "mocha": "^11.7.5",
    "typescript": "^6.0.2"
  }
}
